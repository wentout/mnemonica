# TypeØmatica

This document has moved to [docs/typematica.md](docs/typematica.md).

The non-ASCII filename caused issues on Windows and case-insensitive
filesystems; the canonical location is now under `docs/` with an ASCII
name. This stub remains so that existing external links do not 404.
